---
- name: Reset AD User Password
  hosts: AD
  gather_facts: no

  vars_prompt:
    - name: target_user
      prompt: "Digite o sAMAccountName (ex: loide.grilo)"
      private: no
    - name: new_password
      prompt: "Digite a nova senha temporária"
      private: yes

  vars:
    # Como o seu domínio termina em .lan, ajuste aqui para garantir o UPN correto
    domain_name: "hmiam.minsa.lan"

  tasks:
    - name: 1. Buscar usuário no AD
      microsoft.ad.object_info:
        filter: "sAMAccountName -eq '{{ target_user }}'"
      register: ad_user_info

    - name: 2. Validar existência (Check de segurança)
      fail:
        msg: "ERRO: O usuário '{{ target_user }}' não foi encontrado."
      # Verificamos se a lista 'objects' retornada pelo seu AD está vazia
      when: ad_user_info.objects | length == 0

    - name: 3. Reset password e forçar troca
      microsoft.ad.user:
        # AQUI ESTÁ O AJUSTE: Usando 'DistinguishedName' com D e N maiúsculos
        identity: "{{ ad_user_info.objects[0].DistinguishedName }}"
        password: "{{ new_password }}"
        update_password: always
        password_expired: yes
        account_locked: no
        state: present
      register: password_result

    - name: 4. Resultado Final
      debug:
        msg: "Sucesso! Senha de '{{ target_user }}' resetada. O usuário deve trocar a senha no próximo login."
      when: password_result.changed
